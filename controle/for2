const notas = [6.5, 4.5, 2.4, 6.5, 2.3]

for( let i in notas){
    console.log(i, notas[i])
}

const pessoa = {
    nome: 'Ana',
    sobrenome: 'Barbara',
    idade: 17,
    peso: 55,
}

for(let atributo in pessoa){
    console.log(`${atributo} = ${pessoa[atributo]}`)
}